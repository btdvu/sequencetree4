# context Directory

Welcome, AI Agent!

This directory contains static context descriptions regarding the repository architecture and formats:
* Check [mri_simulation_formats.md](file:///context/mri_simulation_formats.md) for detailed explanation of the binary formats for simulated waveforms and raw template output, including parsing scripts.
* Check [docker_environment.md](file:///context/docker_environment.md) for the role of the Docker environment, pre-installed dependencies, and scripts comparing user/agent modes.

## Rule Reminder:
You are only allowed to edit/write files within the `/.agents/` or `/agentic_coding/` directories (except for modifying sequence definitions under `/sequences/`).
