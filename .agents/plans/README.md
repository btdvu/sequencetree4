# plans Directory

Welcome, AI Agent!

This directory outlines templates and roadmap plans for optimizing pulse sequences:
* Check [feedback_loop_template.md](file:///plans/feedback_loop_template.md) for the detailed step-by-step cycle to automatically modify, compile, simulate, and check sequence timing.

## Rule Reminder:
You are only allowed to edit/write files within the `/.agents/` or `/agentic_coding/` directories (except for modifying sequence definitions under `/sequences/`).
